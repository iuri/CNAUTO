-- /packages/ivc-core/sql/postgresql/ivc-core-create.sql

CREATE TABLE ivc_users (
       test_id	       integer
);

		       --CONSTRAINT ivc_users_user_id_fk
		       --REFERENCES users
       		       --CONSTRAINT ivc_users_user_id_pk
		       --PRIMARY KEY
		       
       